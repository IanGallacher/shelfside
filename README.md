# Static website generator for Shelfside

## Getting started

First, use `asdf` or similar to install the latest ruby.

Finally, run the following commands:

- `gem install jekyll bundler`
- `npm i`
- `jekyll serve --livereload`
